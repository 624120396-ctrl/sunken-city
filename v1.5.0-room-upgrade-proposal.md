# 沉没之城 v1.5.0 房间系统升级方案

## 版本代号
**深渊剧场 (Abyss Theater)**

## 核心理念
> 房间不再只是聊天室，而是一场跑团的「舞台」。

v1.5.0 的目标是让 KP 能「导演」氛围、控制叙事节奏、降低操作负担；让玩家能沉浸其中、快速决策、减少页面跳转。

---

## 一、当前房间系统的短板

| 短板 | 影响 |
|------|------|
| 场景描述只是纯文本 | KP 需要不断打字烘托氛围，累且效果弱 |
| 线索仅存本地 `localStorage` | 玩家换设备或刷新后线索消失，且无法共享 |
| 战斗轮只有列表 | 回合推进全靠记忆，没有清晰的「当前回合高亮」和「行动计时」|
| 骰子结果淹没在聊天记录 | 难以回溯检定历史，无法快速做对抗判定 |
| 角色状态全靠心算 | HP/MP/SAN 变化分散在各条消息里，没有实时面板 |
| 没有 NPC 支持 | KP 只能用自己的账号扮演NPC，缺乏临时角色卡 |
| 音乐/氛围完全缺失 | 跑团最吃氛围的行业痛点，目前为零 |

---

## 二、v1.5.0 功能蓝图（分 3 个阶段）

### Phase 1：舞台化房间基建（优先级：最高）
**让房间看起来像一场真正的跑团桌游界面。**

#### 1.1 全景式房间布局重构
将 `RoomPage.tsx` 从「聊天记录主导」改为「三分栏沉浸式布局」：

```
┌─────────────────────────────────────────────────────────────┐
│  [场景卡] 迷雾笼罩的阿卡姆图书馆...         [氛围控制] [BGM] │  ← 舞台区（顶部 15%）
├──────────────┬──────────────────────────────┬───────────────┤
│              │                              │               │
│   角色面板    │     叙事/聊天记录             │   骰子剧场    │
│   (30%)      │      (50%)                   │   (20%)       │
│              │                              │               │
│  [HP/MP/SAN] │   骰子结果浮层卡片显示        │  [历史记录]   │
│  [快捷技能]   │   聊天区(支持IC/OC/旁白)     │  [快速对抗]   │
│  [状态标记]   │                              │  [自动计算]   │
│              │                              │               │
└──────────────┴──────────────────────────────┴───────────────┘
```

**适用性**：响应式 — 移动端自动折叠为「场景卡 + 聊天 + 底部快捷面板」

#### 1.2 实时角色状态面板（Player HUD）
- 每位玩家进入房间后，左侧固定显示自己的调查员 HUD：
  - 圆形 HP/MP/SAN 仪表盘（类似血球，红/蓝/紫三色）
  - 当前武器、护甲、快捷技能（沿用 `QuickRollBar`，但搬到房间常驻）
  - 状态 tag（受伤、中毒、临时疯狂、昏迷等），KP 可点击添加/移除
- **重要**：这个面板的数据源直接绑定后端 `Character`，KP 用「理智侵蚀」功能扣 SAN 时，仪表盘要**即时动画下降**。

#### 1.3 骰子剧场（Dice Theater）
- 右侧固定「骰子历史」面板，显示当前房间内最近 20 次检定
- 新增 `DiceRoll` 轮播高亮：当有人投骰时，在聊天区上方短暂弹出一个「帷幕卡片」，展示：
  - 投掷者头像
  - 技能名 + 目标值
  - 骰子结果（数字放大）
  - 成功度颜色（极大成功=金色光芒，大失败=血红碎裂）
- 支持**对抗检定**：KP 或玩家可以向另一名玩家发起对抗请求，系统自动把双方的 `DiceRoll` 记录并排显示胜负。

#### 1.4 场景卡升级（Scene Engine）
- `Room.atmosphere` 从 `normal/dark/horror/mystery/warm` 扩充为可切换的**主题滤镜**：
  - 每个 `atmosphere` 对应不同的 CSS 渐变遮罩（比如 `horror` = 暗红血管纹理 + 轻微噪点）
- `Room.sceneDesc` 改为支持**富文本 + 场景头图 URL**，KP 可以上传/粘贴一张氛围图作为场景背景
- 增加**场景切换动画**：KP 修改场景描述时，全员页面淡入淡出过渡
- 新增 `Room.sceneMusic` 字段：KP 可粘贴 BGM 链接（网易云/Spotify/YouTube），前端用 iframe/audio 播放器同步播放（不强求严格同步，但状态同步）

---

### Phase 2：叙事与调查工具（优先级：高）
**解决 KP 表达难、PL 参与弱的痛点。**

#### 2.1 公共线索板（Persistent Clue Board）
**这是目前体验最断裂的地方。**

- 将 `ClueBoard` 从 `localStorage` 搬到了数据库：
  - 新增 `RoomClue` 表：
    ```prisma
    model RoomClue {
      id          String  @id @default(uuid())
      roomId      String
      title       String
      content     String
      imageUrl    String?
      discoveredBy String // userId 或 'all'
      isHidden    Boolean @default(false)
      requiresSkill String? // 如 'spot_hidden'
      requiresValue Int?    // 如 50
      createdAt   DateTime @default(now())
    }
    ```
- KP 操作：
  - 点击聊天记录中的任意消息「标记为线索」，自动生成 `RoomClue`
  - 也可以手动新建线索卡片
  - 可设置「隐藏线索」：需要玩家通过特定技能检定达标后才可见
- 玩家操作：
  - 右侧/底部新增「线索簿」抽屉
  - 已发现的线索以卡片墙形式展示
  - 支持按时间线或按标签分类

#### 2.2 NPC 快速生成器
- KP 在房间内可以创建临时 NPC（不占用账号）：
  - 姓名、头像（可选）、简短描述、关键属性（STR/DEX/CON/HP等）
  - NPC 以「虚拟成员」身份加入战斗轮
  - NPC 可以被分配到某条消息中（比如「深潜者A 攻击了你」）
- 数据存储在 `RoomNpc` 表中，房间关闭后可归档。

#### 2.3 IC / OC / 旁白 三模式聊天
当前聊天只有纯文本。增加消息类型：
- **IC（In Character）**：玩家以调查员身份说话，名字前显示角色名，消息气泡带金色细边框
- **OC（Out of Character）**：玩家本人说话，正常灰色气泡
- **Narration（旁白）**：KP 描述场景，消息呈满宽、斜体、淡金色，无边框
- 快捷切换栏放在输入框上方，默认上次选择。

#### 2.4 SAN 危机可视化
- 当 KP 使用「理智侵蚀」时，触发**全屏理智危机动画**：
  - 受害玩家视角：屏幕边缘红光闪烁 + 心跳音效（可选）
  - 全体视角：系统旁白「某某的调查员似乎看到了什么不可名状之物…」
- 临时疯狂结果以「幕间卡片」形式弹出，显示持续轮数和症状

---

### Phase 3：战斗轮与自动化（优先级：中）
**让战斗不再用 Excel/纸笔心算。**

#### 3.1 战斗轮可视化重构
- 行动顺序不再是文本列表，而是**头顶轮盘条**：
  - DEX 从高到低横向排列角色头像
  - 当前回合者头像放大 + 金色光环
  - 已完成行动者头像变灰
  - 鼠标悬停显示角色简要状态（HP/SAN/状态标记）
- 新增「强制结束回合」与「跳过 NPC」按钮，只有 KP 能操作

#### 3.2 房间级状态效果引擎
- 把 `RoomMember.statusTags` 从 JSON 字符串扩展为**可计算的状态效果系统**：
  - `bleeding` → 每回合结束时自动扣 1 HP
  - `poisoned` → 每回合扣 1 HP + DEX 检定 halved
  - `stunned` → 自动跳过一回合
  - `insane` → 无法区分友敌（系统提示 KP）
- KP 可以给战斗者拖拽添加状态 token，系统在下个回合开始时广播自动结算结果

#### 3.3 快捷动作预设
- 玩家在自己的 HUD 上可以直接点击「射击 / 躲避 / 逃跑 / 治疗自己」，自动投对应骰并发送结果
- 这些动作绑定到 `COC7E_SKILLS`，不需要 KP 反复强调规则

---

## 三、数据库变更

新增 3 张表，扩展 1 张表：

```prisma
model RoomClue {
  id           String   @id @default(uuid())
  roomId       String
  title        String
  content      String
  imageUrl     String?
  discoveredBy String   @default("all")
  isHidden     Boolean  @default(false)
  requiresSkill String?
  requiresValue Int?
  createdAt    DateTime @default(now())
  room         Room     @relation(fields: [roomId], references: [id], onDelete: Cascade)
  @@index([roomId])
}

model RoomNpc {
  id          String   @id @default(uuid())
  roomId      String
  name        String
  avatarUrl   String?
  description String?
  statsJson   String   @default("{}")
  isActive    Boolean  @default(true)
  createdAt   DateTime @default(now())
  room        Room     @relation(fields: [roomId], references: [id], onDelete: Cascade)
  @@index([roomId])
}

model RoomScene {
  id          String   @id @default(uuid())
  roomId      String
  title       String
  description String?
  imageUrl    String?
  musicUrl    String?
  atmosphere  String   @default("normal")
  isActive    Boolean  @default(false)
  createdAt   DateTime @default(now())
  room        Room     @relation(fields: [roomId], references: [id], onDelete: Cascade)
  @@index([roomId])
}
```

同时扩展 `Room`：
```prisma
model Room {
  // ...现有字段
  currentSceneId String?
  sceneMusic     String?
}
```

---

## 四、工作量与排期估算

| 阶段 | 工时估算 | 人力 |
|------|---------|------|
| Phase 1 | 3~4 天 | 1 人（你） |
| Phase 2 | 4~5 天 | 1 人（你） |
| Phase 3 | 3~4 天 | 1 人（你） |
| **总计** | **10~13 天** | **约 2 周** |

如果只有一人单干，建议按 **Phase 1 → 小范围测试 → Phase 2 → Phase 3** 的顺序逐步释放。

---

## 五、最想立刻开始的 3 件事

如果只能先选一个子集作为 v1.5.0 的 MVP，我最建议先做这三项：

1. **左侧角色 HUD + 状态仪表盘**
   — 这是玩家体验提升最直观的改动，也直接利用了已做好的 `Character` 数据。

2. **公共线索板（Persistent Clue Board）**
   — 解决当前 `localStorage` 最大的槽点，是 COC 跑团的核心诉求之一。

3. **IC/OC/Narration 三模式聊天 + 场景卡富文本升级**
   — 成本最低、氛围提升最高，几乎不改动后端 schema 就能先跑起来。

---

## 六、参考风格

- **氛围参考**：《克苏鲁的呼唤 第七版》官方模组的场景描写
- **UI 参考**：Foundry VTT 的 Theater of Mind 模式、Owlbear Rodeo 2.0 的简洁暗色界面
- **交互参考**：D&D Beyond 的角色 HUD 面板、Roll20 的 Turn Tracker

---

如果你同意方向，我可以立刻把「v1.5.0 MVP 三件套装」拆成具体的代码任务清单，并从 `Room` schema 变更开始写。