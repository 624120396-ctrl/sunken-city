# 沉没之城 PRD

**版本：** v1.2
**日期：** 2026-07-10
**项目：** 沉没之城 / Sunken City  
**用途：** 供 Codex 查阅、修订、拆任务和执行；供用户用自然语言指挥 Codex 持续迭代产品。  
**当前阶段：** 房间、调查体验、UI/UX、招募/帮助、版本化发布与真人语音 V1 已完成基础交付；当前进入“生产稳定保护 + VTT 调查场景 MVP”阶段。语音真人听感验收待 Codex Chrome bridge 修复后补做。
**线上入口：** https://coc.city/  
**正确生产服务器：** `64.90.30.232`  

---

## 1. 文档定位

本文不是企业流程型 PRD，不追求复杂的团队审批、商业 KPI 或埋点体系。

本文的目标是让 Codex 在每次接手《沉没之城》时能够快速判断：

1. 产品到底是什么。
2. 当前已经完成了什么。
3. 哪些模块可以继续做。
4. 哪些模块必须先问用户。
5. 哪些区域不能碰。
6. UI/UX 应该朝什么方向演进。
7. 下一步最适合执行什么。

本文可以被 Codex 持续修订。若用户后续明确确认了新的产品规则、视觉方向或功能边界，以用户最新确认内容为准，并同步更新本文。

相关执行文档：

- `PRODUCT.md`
- `README.md`
- `docs/room-system-lifecycle-update-2026-07-03.zh-CN.md`
- `docs/superpowers/plans/2026-07-04-sunken-city-luminous-archive-visual-upgrade.zh-CN.md`
- `docs/superpowers/plans/2026-07-04-sunken-city-ui-asset-pipeline.zh-CN.md`
- `docs/superpowers/plans/2026-07-04-sunken-city-ui-document-audit.zh-CN.md`
- `docs/superpowers/plans/2026-07-04-sunken-city-ui-ux-product-design-master-plan.zh-CN.md`

## 2. 产品一句话

《沉没之城》是一个围绕《克苏鲁的呼唤》跑团体验构建的 Web 平台。

它不是单纯的角色卡工具，也不是普通论坛。它应该像一个可长期使用的“深海秘仪档案馆”：玩家在这里创建调查员、加入房间、跑团、投骰、记录战斗、完成结算、保存报告，并逐步沉淀角色经历、收藏、印记、位阶和社区记忆。

## 3. 产品体验目标

产品体验优先级如下：

1. **跑团能顺畅进行。** 房间、聊天、骰点、战斗、角色状态、结算和报告必须清楚可靠。
2. **KP 在 PC 上能高效主持。** 成员、场景、线索、NPC、骰点、战斗、私聊、结算入口要有清晰分区。
3. **PL 在移动端能轻松参与。** 聊天、骰点、自己的角色状态优先；复杂工具进入抽屉或底部 sheet。
4. **观察者低干扰观看。** 能看公开内容，不能误以为自己可以操作。
5. **长期玩家愿意留下。** 论坛、个人资料、角色档案、位阶、印记、收藏、无名集市要有“档案馆式留存感”。
6. **视觉要精致、明亮、自然。** 减少 AI 味儿、泛用黑色透明卡片、灰底灰字和廉价恐怖风。

成功标准不以商业 KPI 衡量，而以产品体验判断：用户能保持沉浸，同时仍能清楚阅读、快速操作、理解状态、避免误操作。

## 4. 核心用户

| 用户 | 主要设备 | 核心任务 | 体验重点 |
| --- | --- | --- | --- |
| KP / 主持人 | PC | 创建房间、管理成员、推进场景、管理线索/NPC/战斗/结算 | 高信息密度、权限清楚、操作效率 |
| PL / 调查员 | 移动端、PC | 加入房间、阅读聊天、参与骰点、查看角色状态 | 参与优先、少遮挡、低学习成本 |
| OBSERVER / 观察者 | PC、移动端 | 观看公开内容、了解房间状态 | 只读、低干扰、边界明确 |
| 普通浏览用户 | PC、移动端 | 查看角色、社区、市场、个人资料 | 入口清晰、视觉吸引、长期留存 |
| 管理员 | PC | 管理用户、内容、系统资源 | 稳定、安全、可扫描 |

后台 / admin 当前不是本轮 UI/UX 重构重点，除非用户明确要求，否则暂不碰。

## 5. 产品原则

1. 跑团参与优先：房间内聊天、骰点、角色状态和场景推进优先于装饰。
2. 权限契约优先：新 UI 根据 `myRole`、`myCapabilities`、`myBinding`、`lifecycle` 渲染，不从 `isCreator` 或页面局部状态猜权限。
3. 背景负责氛围，Surface 负责可读性。
4. PC 可以沉浸且高密度，但必须可扫描。
5. 移动端不是 PC 缩小版，PL 的核心任务必须优先。
6. 官方规则相关功能必须先明确规则，再实现。
7. 视觉升级必须减少黑色透明卡片、灰底灰字和逐页补丁。
8. image2 资产必须受 UI 规格约束，不能把概念图直接塞进生产 UI。
9. 能通过前端解决的问题先在前端解决；需要新增后端字段或规则时，先写需求说明再执行。
10. 当前默认以 CoC7 为主；文字与真人房间语音 V1 已上线。VTT 第一阶段只做现有调查体验的可选视觉层，不实现完整多规则自动化、AI 语音、TTS、STT 或异步团专门流程。

## 6. 当前已完成内容

### 6.1 基础系统

已具备：

- JWT 用户认证。
- 注册、登录、当前用户信息。
- React + TypeScript + Vite 前端。
- Node.js + Express + Prisma + SQLite 后端。
- Socket.io 实时通信基础。
- 上传和静态资源能力。

### 6.2 角色卡系统

已具备：

- CoC7 基本属性。
- HP / MP / SAN / MOV / Build 等派生属性。
- 技能、武器、护甲等角色数据。
- 角色卡创建、查看、编辑。
- 角色卡导入/导出能力。
- 旧手动战后成长入口。

当前限制：

- “结团后的正式角色成长机制”尚未实施。
- 相关规则已冻结，等待用户整理最终规则文档后再进入实现。

### 6.3 房间系统

已完成兼容式升级：

- `OWNER_KP`
- `KP`
- `PLAYER`
- `OBSERVER`
- `NON_MEMBER`
- `myRole`
- `myCapabilities`
- `myBinding`
- `lifecycle`
- 角色锁。
- 结算草案。
- 结团写回。
- 报告权限。

房间生命周期：

- `WAITING`
- `IN_PROGRESS`
- `PAUSED`
- `FINISHING`
- `COMPLETED`
- `CANCELED`

关键能力：

- KP 创建房间。
- PL 加入房间并绑定角色。
- 观察者观看公开内容。
- 开场后角色锁定。
- 结团或取消后角色释放。
- 结算阶段保存草案。
- 结团时写回确认或批准的最终状态。
- 已结团成员可查看相关报告。
- 非成员不可读取无权限报告。

### 6.4 实时跑团能力

已具备：

- 房间聊天。
- 公开骰。
- 秘密骰。
- 战斗相关状态。
- 房间成员列表。
- 事件 / 日志基础。

当前限制：

- 没有改 Socket 事件格式。
- 战斗状态仍未完整迁移到持久化战斗模型。
- 私聊系统仍按现有数据流保留。

### 6.5 UI/UX 基础

已具备：

- `Surface`
- `PageShell`
- `ReadablePanel`
- `ActionCard`
- `DataCard`
- `tokens-v2.css`
- `system-v2.css`
- `background-v2.css`
- 背景可读性档位。
- section spacing 防止 Surface 粘连。

已确认新方向：

- 使用 Codex 官方内置 image2 生成视觉草稿和纹理资产。
- 暂时不使用 `cd-image2`。
- 先做纹理资产和材质化 Surface，再迁移页面。

## 7. 模块总览

| 模块 | 当前状态 | 产品目标 | 下一步 |
| --- | --- | --- | --- |
| 首页 / 故事书 | 房间列表已升级，首页与故事书入口待专项收口 | 第一眼建立“明亮深海秘仪档案馆”气质 | 待 VTT 入口归属明确后收口 |
| 角色卡 | 基础能力与本轮视觉升级已完成 | 像调查员档案，不只是表单 | 维护修补，官方成长继续冻结 |
| 房间页 | 桌面 KP/PL 定版，移动端聊天优先版上线 | PC 是 KP 控制台，移动端是 PL 参与端 | 只做明确缺陷修补和 VTT 可选入口 |
| 房间报告 | 已接入权限与结算 | 成为可回看的跑团档案 | 强化档案感、权限提示和结算阅读 |
| 聊天 / 骰点 / 战斗 | 可运行 | 状态清楚、危险语义明确 | 不改规则，优先优化前端呈现 |
| 黑水港 | 待视觉与玩法化升级 | 等待、咬钩、收竿、渔获结算、收藏账本 | 做成轻量可重复游玩的小游戏 |
| 溺者之牌 | 待视觉与玩法化升级 | 入梦、选牌、翻牌、解牌、历史图鉴 | 做成仪式感强的占卜玩法 |
| 无名集市 | 待视觉升级 | 遗物陈列和藏品账本 | 作为视觉样板推广对象 |
| 论坛 | 待长期留存塑造 | 可归档文献，而不是普通帖子列表 | 提升阅读、分类和档案感 |
| 个人资料 | 待长期留存塑造 | 调查员档案夹 | 结合角色、记录、印记、收藏 |
| 位阶 / 印记 | 待视觉升级 | 馆藏、证章、封印 | 建立稀有度、仪式感和长期目标 |
| VTT 调查场景板 | 架构研究完成并已合并 | 地图、场景、点位与 Token 作为调查体验的可选视觉层 | 从私有素材资产库 V1 开始 |
| 房间实时语音 | V1 已上线，基础设施健康 | 房间内轻量真人语音，不替代文字与骰点 | 待补双浏览器真人听感验收 |
| 后台 / admin | 暂不碰 | 稳定管理 | 用户明确要求前不重构 |

## 8. 当前主要问题

### 8.1 产品文档曾经分散

此前没有统一 `PRD.md`，产品信息分散在 `PRODUCT.md`、`README.md`、设计计划、房间交付说明和 UI/UX 审计文档中。

本文用于建立 Codex 可执行的产品基线。

### 8.2 房间页体量过大

`RoomPage.tsx` 承载聊天、成员、战斗、工具、生命周期、结算、移动端抽屉等多个职责。

后续应拆分为：

- `RoomShell`
- `RoomStage`
- `RoomParticipantRail`
- `KpWorkbench`
- `PlayerConsole`
- `ObserverConsole`

### 8.3 UI 仍有旧视觉债务

需要逐步减少：

- `bg-black/*` 主内容容器。
- 黑色透明卡片。
- 随机硬编码颜色。
- 大软阴影。
- 页面级补丁。
- 灰底灰字。
- 低透明金色正文。

### 8.4 正式结团成长机制未定

这是规则敏感功能。当前只保留草案和计划，不进入实现。

## 9. Codex 执行边界

### 9.1 可以直接执行

在不改变后端业务规则的前提下，Codex 可以直接执行：

- 前端 UI/UX 重构。
- 页面布局优化。
- 组件拆分。
- CSS token 整理。
- Surface 材质化。
- image2 素材规格卡撰写。
- image2 纹理生成后的压缩、命名、入库和引用。
- 首页、故事书、无名集市、黑水港、溺者之牌、论坛、个人资料等前端视觉升级。
- 房间页前端结构拆分，但必须遵守现有房间契约。
- 轻量截图验收和基础页面检查。

Codex 可自主决策的范围：

- 纯 CSS 和视觉样式调整。
- 不改变功能含义的组件拆分。
- 纹理替换、token 引用、布局密度调整。
- 文档补充和轻量一致性修订。

### 9.2 执行前必须先问用户

以下事项需要先形成说明并等待用户确认：

- 新增后端字段。
- 新增数据库表。
- 新增或重排核心路由。
- 改全局状态管理方式。
- 新增会影响多个页面的组件 API。
- 改接口契约。
- 改 Socket 事件名或事件格式。
- 改骰点规则。
- 改战斗规则。
- 改私聊数据流。
- 改生命周期写回逻辑。
- 正式实现结团后的角色成长机制。
- 支持 CoC7 之外的规则系统。
- 扩展真人语音 V1 范围，包括录音、转写、AI 总结、跨房间大厅或视频。
- 涉及生产部署、回滚、迁移或密钥处理的操作。

### 9.3 当前不碰

除非用户明确要求，当前不做：

- 后台 / admin 重构。
- 房间后端业务逻辑大改。
- Socket 事件格式变更。
- 骰点规则改写。
- 战斗规则改写。
- 私聊数据流重构。
- 生命周期写回逻辑重构。
- 正式结团后角色成长实现。
- 生产部署。
- 深度 E2E、深度 Playwright、全量视觉回归。
- 提交 `outputs/`。

## 10. 核心产品需求

### 10.1 房间与跑团

用户故事：

- 作为 KP，我要创建房间并邀请 PL 加入。
- 作为 PL，我要绑定自己的角色卡加入房间。
- 作为观察者，我要不绑定角色卡观看公开内容。
- 作为 KP，我要控制开场、暂停、进入结算、结团或取消。
- 作为相关成员，我要在结团后仍能看到自己的历史房间和报告。

验收标准：

- 身份和按钮显示来自 `myCapabilities`。
- 非成员不能读取无权限报告。
- 已结团和已取消房间不能继续普通推进。
- 角色锁在开场后生效，在结团或取消后释放。

### 10.2 聊天、骰点与战斗

用户故事：

- 作为 PL，我要在房间内优先看到聊天和骰点记录。
- 作为 KP，我要能公开骰、秘密骰、控制战斗、查看战斗记录。
- 作为移动端 PL，我要少层级参与游戏，不被工具面板遮挡。

验收标准：

- 移动端主聊天和骰点记录优先。
- 危险和战斗状态使用血红，但不得大面积铺底。
- 不改变既有骰点成功度算法。
- 不改变既有 Socket 事件名。

### 10.3 结算与报告

用户故事：

- 作为 KP，我要在结算中为每位调查员保存结算草案。
- 作为 KP，我要确认结算并结团写回角色最终 HP / MP / SAN。
- 作为成员，我要在报告页看到自己相关结算。
- 作为非成员，我不能读取房间结算和 KP 备注。

验收标准：

- 结算只在 `FINISHING` 状态可编辑。
- 结团写回只应用确认或批准且未应用过的结算项。
- 报告页权限使用 `canViewPublicContent`。

### 10.4 角色卡与成长

用户故事：

- 作为 PL，我要创建、查看、编辑自己的调查员。
- 作为 PL，我要在未来能从已结团房间进入正式成长流程。
- 作为 KP，我要能在报告中查看成长结果。

当前状态：

- 手动成长入口保留。
- 正式房间结团成长机制冻结。

验收标准：

- 未确认正式规则前，不实现结团后角色成长。
- 后续实现必须与房间结算、角色锁、报告页打通。

### 10.5 长期留存系统

目标：

- 论坛像可归档文献。
- 个人资料像调查员档案夹。
- 位阶和印记像馆藏、证章、封印。
- 无名集市像遗物陈列和藏品账本。
- 黑水港和溺者之牌作为游戏化试验田。

验收标准：

- 页面视觉统一到明亮材质化系统。
- 长文内容可读。
- 移动端不因装饰牺牲主任务。

## 11. UI/UX 方向

### 11.1 主方向

当前确认方向：**更明亮的深海秘仪档案馆**。

关键词：

- 明亮档案桌。
- 深海日光与冷雾。
- 档案纸与纸张纤维。
- 浅色湿石与矿物面板。
- 氧化铜与旧金属边框。
- 克制旧金仪式感。
- 少量血红危险语义。
- 非纯黑克苏鲁。
- 精致、华丽、自然、可长期使用。

明确避免：

- 全局黑幕。
- 泛用黑色透明卡片。
- 装饰性玻璃拟态作为默认风格。
- 灰底灰字。
- 低透明金色正文。
- 大面积血红恐怖背景。
- 过度圆角和大软阴影。
- 纯概念海报式 UI。

### 11.2 页面结构方向

PC 端建议：

- 左侧：深海金属导航。
- 中央：明亮档案纸 / 浅色湿石主工作区。
- 右侧：深色但非纯黑的工具台。
- 背景：深海冷雾与水下城市氛围，不承载正文。

移动端建议：

- 主任务优先展示。
- 次要工具进入抽屉、sheet 或折叠区。
- 避免把 PC 侧栏直接堆叠到手机页面。
- 聊天、骰点、自己角色状态优先级最高。

### 11.3 Surface 材质系统

建议 `Surface` 增加 `material` 维度：

- `archive`：档案纸、正文、阅读区。
- `limestone`：浅色湿石、主工作区。
- `basalt`：深色石材、工具台、暗面板。
- `copper`：氧化铜、边框、导航、仪式结构。
- `relic`：遗物、收藏、特殊高价值内容。

职责：

- `variant` 表示层级和用途。
- `material` 表示视觉材质。
- `tone` 表示语义强调。

### 11.4 image2 资产规范

必须遵守：

- 先写资产规格卡。
- 再生成候选。
- 候选筛选通过后才能处理和入库。
- 入库后通过 CSS token 或组件 API 使用。
- 页面不得直接引用未登记图片路径。
- 纹理必须为 UI 服务，不能抢正文、不能像概念海报。

标准资产：

- 明亮档案纸。
- 浅色湿石。
- 氧化铜边框。
- 深海冷雾。
- 暗釉石材。
- 旧金箔细纹。

## 12. 数据与权限边界

### 12.1 房间前端契约

新 UI 必须优先使用：

- `myRole`
- `myCapabilities`
- `myBinding`
- `lifecycle`

旧字段 `isCreator` 和 `isMember` 只作为兼容字段，不应作为新 UI 权限判断主依据。

### 12.2 写操作边界

以下能力必须由后端 capability 或权限 helper 约束：

- KP 笔记。
- 线索。
- NPC。
- 阶段 / 场景。
- 子房间。
- 日志 / 事件。
- 战斗控制。
- 公开发言。
- 公开骰。
- 秘密骰。
- 结算工作台。

### 12.3 数据安全

不得提交：

- `.env`
- 数据库文件。
- uploads。
- 生产密钥。
- `outputs/`
- 未压缩大型临时图片。

## 13. 页面优先级

### P0：当前最优先

1. 生产 release、房间权限、暗骰脱敏和语音基础设施稳定。
2. 语音双浏览器真人听感补验；工具环境缺口不视为服务失败。
3. VTT 私有素材资产库 V1 实施计划。

### P1：VTT 调查场景 MVP

1. 私有素材资产库 V1。
2. RoomScene 背景与 marker 点位 V1。
3. TokenAsset / SceneTokenInstance 绑定 V1。
4. RuleSystemManifest / RuleRegistry V1。
5. FVTT / Roll20 只读 pack adapter V1。

### P2：可并行的产品收口

1. 招募板 V1.1 与新人引导。
2. 首页、故事书入口与全局导航专项。
3. 运行监控、证书续期与权限回归保护。

### 冻结

1. 结团后的正式角色成长机制。
2. 后台 / admin UI/UX 重构。
3. 房间后端业务规则重写。
4. AI 语音、TTS、STT、语音转文字、录音和跨房间语音大厅。

### 当前独立专项

1. VTT 调查场景 MVP：研究完成，按五个依赖切片推进，第一项为私有素材资产库 V1。
2. 房间实时语音频道 V1：已部署，待补双浏览器真人听感验收。

## 14. 路线图

### 最小可用里程碑

第一个必须看得见的成果不是全站重构，而是：

```text
首批 image2 纹理
+ Surface 材质化
+ 故事书 / 房间列表样板页
+ 375px、1440px、2560x1080 轻量截图确认
```

达到这个里程碑后，再决定是否继续推广到 P1 页面。

### Phase 0：视觉资产与规格

目标：

- 固化 image2 资产规格。
- 生成并筛选首批纹理。
- 建立 `apps/web/public/ui-textures/`。
- 规定文件命名、压缩、token 引用方式。

完成定义：

- 至少有一组可用于生产 UI 的档案纸、湿石、铜边、冷雾、暗石、旧金纹理。
- 每个资产有用途说明。
- 页面不会直接引用未登记素材。

### Phase 1：材质化设计系统

目标：

- 扩展 `Surface material`。
- 建立 texture token。
- 替换黑色透明卡片的默认表达。
- 让明亮和暗色区域都可读。

完成定义：

- `Surface` 能承载 `archive`、`limestone`、`basalt`、`copper`、`relic` 等材质。
- 14px-16px 中文正文清楚。
- 旧黑色透明卡片显著减少。

### Phase 2：第一个真实样板页

候选：

- 首页。
- 故事书 / 房间列表。

目标：

- 验证明亮档案馆视觉在真实页面可用。
- 验证中文正文可读。
- 验证移动端不崩。
- 验证 21:9 超宽屏不空、不挤。

完成定义：

- 用户看图能确认方向是否继续。
- Codex 能把样板规范推广到其他页面。

### Phase 3：长期留存页面升级

页面顺序：

1. 个人资料 / 调查员档案夹。
2. 无名集市。
3. 论坛。
4. 黑水港。
5. 溺者之牌。
6. 位阶 / 印记。

目标：

- 把功能页面做成“档案馆体验”，而不是普通后台列表。
- 保证页面自然、精致、明亮，不堆黑色透明卡片。

### Phase 4：房间页前端重构

目标：

- 拆分 `RoomPage`。
- PC/KP 成为跑团控制台。
- 移动端/PL 成为参与端。
- OBSERVER 成为低干扰只读端。

建议组件：

- `RoomShell`
- `RoomStage`
- `RoomParticipantRail`
- `KpWorkbench`
- `PlayerConsole`
- `ObserverConsole`

完成定义：

- 不改变后端房间规则。
- 不改变 Socket 事件契约。
- 权限显示来自 `myCapabilities`。
- 移动端 PL 不被 KP 工具压住主任务。

## 15. 轻量验收标准

除非用户主动要求，不做深度测试。默认只做轻量验收。

### 产品验收

- KP、PL、OBSERVER、NON_MEMBER 的身份显示清楚。
- 房间生命周期状态清楚。
- 权限按钮不误导用户。
- 已结团房间和报告权限正确。
- 移动端可完成核心参与任务。

### UI/UX 验收

- 整体比旧版更明亮。
- 主内容不再像黑色透明卡片。
- 14px-16px 中文正文可读。
- 明亮背景和暗色背景都可用。
- 375px 手机无横向溢出。
- 移动端主要触摸目标不小于 48px x 48px。
- 移动端聊天输入框在虚拟键盘弹出时不遮挡核心输入。
- 1440px 桌面信息密度合理。
- 2560x1080 超宽屏不空、不挤。
- 动效表达状态，不做无意义装饰。

### 资产验收

- image2 资产有规格卡。
- 入库资产命名清楚。
- 入库资产经过压缩。
- CSS 通过 token 引用资产。
- 纹理不抢正文。
- 不提交临时候选和 `outputs/`。

### 默认检查方式

默认只做：

- 文档一致性检查。
- TypeScript / lint 的轻量检查，若当前任务涉及代码。
- 必要时做少量截图查看。
- 不主动跑全站 E2E、深度 Playwright 或全量视觉回归。

## 16. Kimi 交接需求

不需要等待 Kimi 交接才能执行本文。现有仓库文档已经足够建立产品基线。

如果 Kimi 或之前开发者能补充以下资料，会提高后续执行准确度：

1. 历史产品决策说明：哪些功能为什么做、为什么搁置。
2. 线上风险清单：已知 bug、脆弱接口、不能动的模块。
3. 未合并或未提交改动说明。
4. 数据库迁移历史和当前 schema 漂移说明。
5. 生产部署流程和回滚流程的最新版本。
6. 房间系统、战斗、私聊、报告的历史设计边界。
7. 官方规则相关资料，尤其结团后的角色成长机制。
8. 旧 UI 设计稿或用户反馈截图。

交接原则：

- 不接收密钥明文。
- 不在聊天中回显生产 secret。
- 如果文档含密钥，只保留“先备份、再轮换、再部署”等操作原则。

## 17. 当前开放问题

1. 结团后的正式角色成长规则等待用户整理。
2. VTT 规则按 room 还是 scenario 启用，待私有素材资产库实施计划前锁定。
3. 后台 / admin 是否纳入后续 UI/UX 重构，当前默认不碰。
4. VTT 坐标模式、`public` 可见性、私有存储位置和视频代理策略待锁定。
5. 需要抽样检查用户真实 FVTT/Roll20 包结构。
6. 语音真人听感验收等待 Codex Chrome native host 修复和两个独立登录身份。

## 18. Codex 下一步入口

当前建议执行顺序：

1. 阅读 `docs/superpowers/research/vtt-architecture/README.zh-CN.md` 与架构映射方案。
2. 锁定规则启用粒度、坐标、可见性、私有存储和视频代理五项决定。
3. 抽样检查用户真实 FVTT/Roll20 包。
4. 制定并审计私有素材资产库 V1 实施计划。
5. 独立实现并验收资产库，再按 RoomScene、Token、RuleRegistry、pack adapter 顺序推进。
6. 语音真人听感验收在 Chrome bridge 修复后单独补做，不阻塞 VTT。

Codex 接手时应优先阅读本文，再阅读相关执行文档。若本文与执行文档冲突，以本文的产品边界和用户最新确认内容为准。
