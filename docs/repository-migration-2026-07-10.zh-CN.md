# 沉没之城本机仓库迁移记录

**迁移日期：** 2026-07-10
**当前开发主仓库：** `Y:\sunkencity`
**回退副本：** `C:\Users\29102\Documents\沉没之城`

## 结论

本机开发根目录已切换至 `Y:\sunkencity`。后续 Codex 线程、Git 操作、本地构建和新建隔离工作树均以该目录为准。

原 C 盘目录在本次切换后只承担回退用途，不得与 Y 盘主仓库并行开发，避免出现同一分支在两个工作目录分别产生提交的情况。

## 切换基线与备份

- 切换提交：`e8943c3 fix: link room list recruitment entry`
- 分支：`codex/frontend-system-v2-phase1`
- Git 备份包：`Y:\sunken-city-pre-migration-20260710.bundle`
- 备份包已通过 Git 完整性校验，可包含完整历史与现有引用。

## 已迁移内容

- 受 Git 管理的源码、文档、配置和提交历史。
- 本地依赖、构建资料、测试资料、临时资料与现有工作目录中的必要非 Git 文件。
- 当前工作目录的 Git 远端配置和分支状态。

## 不继承的内容

旧目录下的 `.worktrees` 没有迁入。这些是与旧 Git 根绑定的隔离工作树，不应复制到新根目录。后续如需并行开发，应从 `Y:\sunkencity` 新建工作树。

历史文档中出现的 C 盘绝对路径保持不变，它们用于说明当时的交付环境；当前协作规则以根目录 `AGENT.md` 为准。

## 回退与清理

- 在下一次正常开发和部署周期确认无误前，保留 C 盘回退副本和 Git bundle。
- 需要回退时，停止 Y 盘写入后，以 C 盘副本或 bundle 恢复；不要将两个目录的未提交改动混合复制。
- C 盘旧目录、旧 `.worktrees` 与旧临时资料的删除或归档，必须在单独确认后执行。
